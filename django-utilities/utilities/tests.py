import utilities
from utilities import decorators, itertools, models, views
from utilities.views import generic
from utilities.templatetags import utils

doctest_modules = (
	utilities,
	decorators,
	itertools,
	models,
	views,
	generic,
	utils,
    models.fields,
)
